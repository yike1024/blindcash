-- 006_spent_coins_key_version.sql — Phase 3 (PostgreSQL)
--
-- In the SQLite lineage this added key_version to spent_coins. On PostgreSQL
-- the baseline already includes it, so this is a no-op.
SELECT 1;
